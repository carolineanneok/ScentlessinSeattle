'use strict';


var clickCounter = 0;
var clicks = document.createElement('button');

var click = allbutton[i].clicks;

var clickArray = [];

function facebookClicks() {
    for (var i = 0; i < allbutton.length; i++) {
      clickArray.push(allbutton[i].clicks);
   }










   //Save the facebook button click counts to local storage.
   //when the page reloads it will show previous counts
   //save the local storage code in the click counter function
document.getElementById("button").addEventListener("mouseover", facebookClicks);{
}
count=localStorage.getItem('facebookClicks');{
}
}

document.getElementById("button").addEventListener("click", function(){
   this.style.backgroundColor = "blue";
 });